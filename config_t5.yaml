paths:
  data_dir: "/data/user_data/anshulk/cafa6/data/"
  raw_train_sequences: "train_sequences.fasta"
  raw_test_sequences: "testsuperset.fasta"
  output_dir: "/data/user_data/anshulk/cafa6/embeddings/"
  log_dir: "/data/user_data/anshulk/cafa6/logs/"

run_params:
  batch_size: 32  # Optimized for A6000 48GB - encoder-only models
  save_every_n_batches: 100
  seed: 42

models:
  prot_t5_xl:
    name: "Rostlab/prot_t5_xl_half_uniref50-enc"  # Encoder-only FP16
    device: 0
    repo_id: "t5"
    output_key: "prot_t5_xl"

  prost_t5:
    name: "Rostlab/ProstT5_fp16"  # Encoder-only FP16 with structural awareness
    device: 1  # Separate GPU for parallel processing
    repo_id: "t5"
    output_key: "prost_t5"
